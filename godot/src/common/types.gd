extends Node

const DEFAULT_SPEED:=200.0
const PILE_SPEED_FACTOR:=.8
const PILE_ACCCEL_FACTOR:=.95
const XP_PER_PRESENT:=20.0
const XP_PER_METER:=1.0

enum GameMusic {EASY, NORMAL, HARD}

enum Obstacle { Log, Snowpile }
